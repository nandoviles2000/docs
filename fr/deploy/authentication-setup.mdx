---
title: "Configuration de l’authentification"
description: "Configurez l’authentification des utilisateurs sur votre site de documentation avec OAuth, JWT ou des liens partagés afin de contrôler l’accès aux pages et à la Référence de l’API."
keywords: ['authentification', 'auth', 'OAuth', 'JWT', 'mot de passe']
---

<Info>
  Les [offres Pro](https://mintlify.com/pricing?ref=authentication) incluent l’authentification par mot de passe.

  Les [offres Enterprise](https://mintlify.com/pricing?ref=authentication) incluent toutes les méthodes d’authentification.
</Info>

<Warning>
  L’authentification est disponible uniquement pour la documentation hébergée sur un domaine personnalisé ou un sous-domaine Mintlify (par exemple, `docs.example.com` ou `example.mintlify.dev`). L’authentification n’est **pas prise en charge** si vous utilisez un [chemin de base personnalisé](/fr/deploy/docs-subpath) (par exemple, `example.com/docs`).
</Warning>

L’authentification impose aux utilisateurs de se connecter avant d’accéder à votre documentation.

Lorsque vous activez l’authentification, les utilisateurs doivent se connecter pour accéder au contenu. Vous pouvez définir des pages ou des groupes spécifiques comme publics tout en gardant les autres pages protégées.

<div id="configure-authentication">
  ## Configurer l’authentification
</div>

Sélectionnez la méthode de négociation que vous souhaitez configurer.

<Tabs>
  <Tab title="Mot de passe">
    <Info>
      L’authentification par mot de passe sert uniquement à contrôler l’accès et ne prend **pas** en charge les fonctionnalités propres à chaque utilisateur, comme le contrôle d’accès par groupe ou le préremplissage du bac à sable API.
    </Info>

    ### Prérequis

    * Vos exigences de sécurité autorisent le partage d’un même mot de passe entre plusieurs utilisateurs.

    ### Configuration

    <Steps>
      <Step title="Créer un mot de passe.">
        1. Dans votre tableau de bord, accédez à [Authentication](https://dashboard.mintlify.com/products/authentication).
        2. Activez l’authentification.
        3. Dans la section **Password Protection**, saisissez un mot de passe sécurisé

        Après avoir saisi un mot de passe, votre site est redéployé. Une fois le déploiement terminé, toute personne qui visite votre site doit saisir ce mot de passe pour accéder à votre contenu.
      </Step>

      <Step title="Partager l’accès.">
        Partagez en toute sécurité le mot de passe et l’URL de la documentation avec les utilisateurs autorisés.
      </Step>
    </Steps>

    ### Exemple

    Vous hébergez votre documentation sur `docs.foo.com` et vous avez besoin d’un contrôle d’accès simple sans suivre les utilisateurs individuellement. Vous souhaitez empêcher l’accès public tout en conservant une configuration simple.

    **Créez un mot de passe fort** dans votre tableau de bord. **Partagez les informations d’accès** avec les utilisateurs autorisés.
  </Tab>

  <Tab title="tableau de bord Mintlify">
    ### Prérequis

    * Toute personne devant accéder à votre documentation doit être membre de votre organisation Mintlify.

    ### Configuration

    <Steps>
      <Step title="Activer l’authentification dans le tableau de bord Mintlify.">
        1. Dans votre tableau de bord, accédez à [Authentication](https://dashboard.mintlify.com/products/authentication).
        2. Activez l’authentification.
        3. Dans la section **Custom Authentication**, cliquez sur **Mintlify Auth**.
        4. Cliquez sur **Enable Mintlify Auth**.

        Une fois l’authentification Mintlify activée, votre site est redéployé. Lorsque le déploiement est terminé, toute personne qui visite votre site doit se connecter à votre organisation Mintlify pour accéder à votre contenu.
      </Step>

      <Step title="Ajouter des utilisateurs autorisés.">
        1. Dans votre tableau de bord, accédez à [Members](https://dashboard.mintlify.com/settings/organization/members).
        2. Ajoutez chaque personne devant avoir accès à votre documentation.
        3. Attribuez les rôles appropriés en fonction de leurs droits de modification.
      </Step>
    </Steps>

    ### Exemple

    Vous hébergez votre documentation sur `docs.foo.com` et toute votre équipe a accès à votre tableau de bord. Vous souhaitez limiter l’accès aux seuls membres de l’équipe.

    **Activez l’authentification Mintlify** dans les paramètres de votre tableau de bord.

    **Vérifiez l’accès de l’équipe** en confirmant que tous les membres de l’équipe sont actifs dans votre organisation.
  </Tab>

  <Tab title="OAuth 2.0">
    ### Prérequis

    * Un serveur OAuth ou OIDC compatible avec le flux Authorization Code.
    * La possibilité de créer un point de terminaison d’API accessible à l’aide de jetons d’accès OAuth (facultatif, pour activer le contrôle d’accès basé sur les groupes).

    ### Configuration

    <Steps>
      <Step title="Configurez vos paramètres OAuth.">
        1. Dans votre tableau de bord, accédez à [Authentication](https://dashboard.mintlify.com/products/authentication).
        2. Activez l’authentification.
        3. Dans la section **Custom Authentication**, cliquez sur **OAuth**.
        4. Configurez les champs suivants :

        * **Authorization URL** : le point de terminaison OAuth.
        * **Client ID** : l’identifiant de votre client OAuth 2.0.
        * **Client Secret** : le secret de votre client OAuth 2.0.
        * **Scopes** (facultatif) : les autorisations à demander. Copiez la chaîne de portée **complète** (par exemple, pour une portée telle que `provider.users.docs`, copiez l’intégralité de `provider.users.docs`). Utilisez plusieurs portées si vous avez besoin de différents niveaux d’accès.
        * **Additional authorization parameters** (facultatif) : les paramètres de requête supplémentaires à ajouter à la requête d’autorisation initiale.
        * **Token URL** : le point de terminaison d’échange de jeton OAuth.
        * **Info API URL** (facultatif) : le point de terminaison sur votre serveur que Mintlify appelle pour récupérer les informations utilisateur. Obligatoire pour le contrôle d’accès basé sur les groupes. S’il n’est pas renseigné, le flux OAuth vérifie uniquement l’identité.
        * **Logout URL** (facultatif) : l’URL de déconnexion native de votre fournisseur OAuth. Lorsque les utilisateurs se déconnectent, Mintlify valide l’URL de redirection de déconnexion par rapport à cette URL configurée, pour des raisons de sécurité. La redirection ne réussit que si elle correspond exactement à la valeur `logoutUrl` configurée. Si vous ne configurez pas d’URL de déconnexion, les utilisateurs sont redirigés vers `/login`. Mintlify redirige les utilisateurs avec une requête `GET` et n’ajoute aucun paramètre de requête. Incluez donc tous les paramètres (par exemple `returnTo`) directement dans l’URL.
        * **Redirect URL** (facultatif) : l’URL vers laquelle rediriger les utilisateurs après l’authentification.

        5. Cliquez sur **Save changes**.

        Après avoir configuré vos paramètres OAuth, votre site est redéployé. Une fois le déploiement terminé, toute personne qui visite votre site doit se connecter à votre fournisseur OAuth pour accéder à votre contenu.
      </Step>

      <Step title="Configurez votre serveur OAuth.">
        1. Copiez la **Redirect URL** depuis vos [paramètres d’authentification](https://dashboard.mintlify.com/products/authentication).
        2. Ajoutez l’URL de redirection comme URL de redirection autorisée pour votre serveur OAuth.
      </Step>

      <Step title="Créez votre point de terminaison d’informations utilisateur (facultatif).">
        Pour activer le contrôle d’accès basé sur les groupes, créez un point de terminaison d’API qui :

        * Répond aux requêtes `GET`.
        * Accepte un en-tête `Authorization: Bearer <access_token>` pour l’authentification.
        * Renvoie les données utilisateur au format `User`. Consultez [User data format](#user-data-format) pour plus d’informations.

        Mintlify appelle ce point de terminaison avec le jeton d’accès OAuth afin de récupérer les informations utilisateur. Aucun paramètre de requête supplémentaire n’est envoyé.

        Ajoutez l’URL de ce point de terminaison dans le champ **Info API URL** de vos [paramètres d’authentification](https://dashboard.mintlify.com/products/authentication).
      </Step>
    </Steps>

    ### Exemple

    Vous hébergez votre documentation sur `foo.com/docs` et vous disposez déjà d’un serveur OAuth sur `auth.foo.com` compatible avec le flux Authorization Code.

    **Configurez les informations de votre serveur OAuth** dans votre tableau de bord :

    * **Authorization URL** : `https://auth.foo.com/authorization`
    * **Client ID** : `ydybo4SD8PR73vzWWd6S0ObH`
    * **Scopes** : `['provider.users.docs']`
    * **Token URL** : `https://auth.foo.com/exchange`
    * **Info API URL** : `https://api.foo.com/docs/user-info`
    * **Logout URL** : `https://auth.foo.com/logout?returnTo=https%3A%2F%2Ffoo.com%2Fdocs`

    **Créez un point de terminaison d’informations utilisateur** à l’adresse `api.foo.com/docs/user-info`, qui nécessite un jeton d’accès OAuth avec la portée `provider.users.docs` et renvoie :

    ```json
    {
      "groups": ["engineering", "admin"],
      "expiresAt": 1735689600,
      "apiPlaygroundInputs": {
        "header": {
          "Authorization": "Bearer user_abc123"
        }
      }
    }
    ```

    <Note>
      Contrôlez la durée de la session à l’aide du champ `expiresAt` dans votre réponse d’informations sur l’utilisateur. Il s’agit d’un horodatage Unix (nombre de secondes écoulées depuis l’époque Unix) indiquant à quel moment la session doit expirer. Consultez [Format des données utilisateur](#user-data-format) pour en savoir plus.
    </Note>

    **Configurez votre serveur OAuth pour autoriser les redirections** vers votre URL de rappel.
  </Tab>

  <Tab title="JWT">
    ### Prérequis

    * Un système d’authentification pouvant générer et signer des JWT.
    * Un service back-end capable de générer des URL de redirection.

    ### Configuration

    <Steps>
      <Step title="Générez une clé privée.">
        1. Dans votre tableau de bord, accédez à [Authentification](https://dashboard.mintlify.com/products/authentication).
        2. Activez l’authentification.
        3. Dans la section **Authentification personnalisée**, cliquez sur **JWT**.
        4. Saisissez l’URL de votre processus de connexion existant.
        5. Cliquez sur **Enregistrer les modifications**.
        6. Cliquez sur **Générer une nouvelle clé**.
        7. Stockez votre clé dans un emplacement sécurisé accessible à votre backend.

        Après avoir généré une clé privée, votre site est redéployé. Une fois le déploiement terminé, tous les visiteurs de votre site devront se connecter à votre système d’authentification JWT pour accéder à votre contenu.
      </Step>

      <Step title="Intégrez l’authentification Mintlify à votre processus de connexion.">
        Modifiez votre processus de connexion existant pour y inclure les étapes suivantes après l’authentification de l’utilisateur :

        * Créez un JWT contenant les informations de l’utilisateur authentifié au format `User`. Consultez [Format des données utilisateur](#user-data-format) pour plus d’informations.
        * Signez le JWT avec votre clé secrète à l’aide de l’algorithme EdDSA.
        * Créez une URL de redirection vers le chemin `/login/jwt-callback` de votre documentation, en incluant le JWT dans le fragment de l’URL.
      </Step>
    </Steps>

    ### Exemple

    Vous hébergez votre documentation sur `docs.foo.com` avec un système d&#39;authentification existant sur `foo.com`. Vous souhaitez étendre votre flux de connexion pour accorder l&#39;accès à la documentation tout en la maintenant séparée de votre tableau de bord (ou vous n&#39;avez pas de tableau de bord).

    Créez un point de terminaison de connexion à l&#39;adresse `https://foo.com/docs-login` qui s&#39;appuie sur votre authentification existante.

    Après vérification des identifiants de l&#39;utilisateur :

    * Générez un JWT avec des données utilisateur au format Mintlify.
    * Signez le JWT, puis redirigez vers `https://docs.foo.com/login/jwt-callback#{SIGNED_JWT}`.

    <CodeGroup>
      ```ts TypeScript
      import * as jose from 'jose';
      import { Request, Response } from 'express';

      const TWO_WEEKS_IN_MS = 1000 * 60 * 60 * 24 * 7 * 2;
      const DOCS_HOST = 'docs.example.com';

      const signingKey = await jose.importPKCS8(process.env.MINTLIFY_PRIVATE_KEY, 'EdDSA');

      export async function handleRequest(req: Request, res: Response) {
        const user = {
          host: DOCS_HOST, // Doit correspondre à l'URL de votre documentation
          expiresAt: Math.floor((Date.now() + TWO_WEEKS_IN_MS) / 1000), // Expiration de session de 2 semaines
          groups: res.locals.user.groups,
          apiPlaygroundInputs: {
            header: {
              "Authorization": `Bearer ${res.locals.user.apiKey}`,
            },
          },
        };

        const jwt = await new jose.SignJWT(user)
          .setProtectedHeader({ alg: 'EdDSA' })
          .setExpirationTime('10 s') // Expiration du JWT de 10 secondes
          .sign(signingKey);

        return res.redirect(`https://${DOCS_HOST}/login/jwt-callback#${jwt}`);
      }
      ```

      ```python Python
      import jwt # pyjwt
      import os

      from datetime import datetime, timedelta
      from fastapi.responses import RedirectResponse

      private_key = os.getenv(MINTLIFY_JWT_PEM_SECRET_NAME, '')
      DOCS_HOST = 'docs.example.com'

      @router.get('/auth')
      async def return_mintlify_auth_status(current_user):
        jwt_token = jwt.encode(
          payload={
            'host': DOCS_HOST, # Doit correspondre à l'URL de votre documentation
            'exp': int((datetime.now() + timedelta(seconds=10)).timestamp()),    # Expiration du JWT : 10 secondes
            'expiresAt': int((datetime.now() + timedelta(weeks=2)).timestamp()), # Expiration de la session : 2 semaines
            'groups': ['admin'] if current_user.is_admin else [],
            'apiPlaygroundInputs': {
              'header': {
                'Authorization': f'Bearer {current_user.api_key}',
              },
            },
          },
          key=private_key,
          algorithm='EdDSA'
        )

        return RedirectResponse(url=f'https://{DOCS_HOST}/login/jwt-callback#{jwt_token}', status_code=302)
      ```
    </CodeGroup>

    ### Rediriger les utilisateurs non authentifiés

    Lorsqu&#39;un utilisateur non authentifié tente d&#39;accéder à une page protégée, la redirection vers votre URL de connexion préserve la destination initialement demandée.

    1. L’utilisateur tente d’accéder à une page protégée : `https://docs.foo.com/quickstart`.
    2. Redirigez vers votre URL de connexion avec un paramètre de requête `redirect` : `https://foo.com/docs-login?redirect=%2Fquickstart`.
    3. Après l’authentification, redirigez vers `https://docs.foo.com/login/jwt-callback?redirect=%2Fquickstart#{SIGNED_JWT}`.
    4. L’utilisateur est redirigé vers sa destination initiale.
  </Tab>
</Tabs>

<div id="make-pages-public">
  ## Rendre les pages publiques
</div>

Lorsque vous utilisez l’authentification, toutes les pages nécessitent une authentification par défaut pour être accessibles. Vous pouvez rendre certaines pages consultables sans authentification au niveau de la page ou du groupe grâce à la propriété `public`.

<div id="individual-pages">
  ### Pages individuelles
</div>

Pour rendre une page publique, ajoutez `public: true` dans les métadonnées d’en-tête de la page.

```mdx Public page example
---
title: "Public page"
public: true
---
```

<div id="groups-of-pages">
  ### Groupes de pages
</div>

Pour rendre toutes les pages d’un groupe publiques, ajoutez `"public": true` sous le nom du groupe dans l’objet `navigation` de votre `docs.json`.

```json Public group example
{
  "navigation": {
    "groups": [
      {
        "group": "Public group",
        "public": true,
        "icon": "play",
        "pages": [
          "quickstart",
          "installation",
          "settings"
        ]
      },
      {
        "group": "Private group",
        "icon": "pause",
        "pages": [
          "private-information",
          "secret-settings"
        ]
      }
    ]
  }
}
```

<div id="control-access-with-groups">
  ## Contrôlez l’accès à l’aide de groupes
</div>

Lorsque vous utilisez l’authentification OAuth ou JWT, vous pouvez restreindre l’accès à certaines pages à des groupes d’utilisateurs spécifiques. C’est utile si vous voulez que différents utilisateurs voient des contenus différents en fonction de leur rôle ou de leurs attributs.

Gérez les groupes à l’aide des données utilisateur transmises lors de l’authentification. Consultez le [format des données utilisateur](#user-data-format) pour en savoir plus.

```json Example user info
{
  "groups": ["admin", "beta-users"],
  "expiresAt": 1735689600
}
```

Indiquez quels groupes peuvent accéder à certaines pages à l’aide de la propriété `groups` dans les métadonnées d’en-tête.

```mdx Example page restricted to the admin group highlight={3}
---
title: "Admin dashboard"
groups: ["admin"]
---
```

Les utilisateurs doivent appartenir à au moins un des groupes listés pour pouvoir accéder à la page. Si un utilisateur tente d’accéder à une page sans appartenir au groupe requis, il recevra une erreur 404.

<div id="how-groups-interact-with-public-pages">
  ### Comment les groupes fonctionnent avec les pages publiques
</div>

* Toutes les pages nécessitent une authentification par défaut.
* Les pages avec une propriété `groups` sont accessibles uniquement aux utilisateurs authentifiés appartenant à ces groupes.
* Les pages sans propriété `groups` sont accessibles à tous les utilisateurs authentifiés.
* Les pages avec `public: true` et sans propriété `groups` sont accessibles à tout le monde.

<CodeGroup>
  ```mdx Page publique
  ---
  title: "Guide public"
  public: true
  ---
  ```

  ```mdx Page protégée
  ---
  title: "Référence de l’API"
  ---
  ```

  ```mdx Page protégée avec groupes
  ---
  title: "Configurations avancées"
  groups: ["pro", "enterprise"]
  ---
  ```
</CodeGroup>

<div id="user-data-format">
  ## Format des données utilisateur
</div>

Lorsque vous utilisez l’authentification OAuth ou JWT, votre système renvoie des données utilisateur qui déterminent la durée de la session, l’appartenance aux groupes et la [personnalisation du contenu](/fr/create/personalization).

<CodeGroup>
  ```tsx Format
  type User = {
    host?: string;
    expiresAt?: number;
    groups?: string[];
    content?: Record<string, any>;
    apiPlaygroundInputs?: {
      server?: Record<string, string>;
      header?: Record<string, unknown>;
      query?: Record<string, unknown>;
      cookie?: Record<string, unknown>;
      path?: Record<string, unknown>;
    };
  };
  ```

  ```json Example
  {
    "host": "docs.example.com",
    "expiresAt": 1735689600,
    "groups": ["admin", "beta-users"],
    "content": {
      "firstName": "Jane",
      "company": "Acme Corp"
    },
    "apiPlaygroundInputs": {
      "header": {
        "Authorization": "Bearer user_abc123"
      },
      "server": {
        "baseUrl": "https://api.foo.com"
      }
    }
  }
  ```
</CodeGroup>

<ParamField path="host" type="string">
  **Obligatoire pour l’authentification JWT.** Le nom d’hôte de votre site de documentation. Cette chaîne doit correspondre exactement au domaine sur lequel vous déployez votre documentation. Mintlify vérifie que l’hôte du JWT correspond à l’hôte de la requête afin d’éviter la réutilisation du jeton entre différents sites.
</ParamField>

<ParamField path="expiresAt" type="number">
  Heure d’expiration de la session, en secondes depuis l’époque Unix. Lorsque l’heure actuelle dépasse cette valeur, l’utilisateur doit se réauthentifier.

  <Warning>**Pour JWT :** cette valeur est différente du claim `exp` du JWT, qui détermine à partir de quand un JWT est considéré comme invalide. Définissez le claim JWT `exp` sur une courte durée (10 secondes ou moins) pour des raisons de sécurité. Utilisez `expiresAt` pour la durée réelle de la session (de quelques heures à plusieurs semaines).</Warning>
</ParamField>

<ParamField path="groups" type="string[]">
  Liste des groupes auxquels l’utilisateur appartient. Les pages dont les métadonnées d’en-tête contiennent des `groups` correspondants sont accessibles à cet utilisateur.

  **Exemple** : un utilisateur avec `groups: ["admin", "engineering"]` peut accéder aux pages associées aux groupes `admin` ou `engineering`.
</ParamField>

<ParamField path="content" type="Record<string, any>">
  Données personnalisées accessibles dans les pages MDX via la variable `user` pour le [contenu personnalisé](/fr/create/personalization#dynamic-mdx-content).
</ParamField>

<ParamField path="apiPlaygroundInputs" type="object">
  Préremplit les champs du bac à sable API avec des valeurs propres à l’utilisateur. Lorsqu’un utilisateur s’authentifie, ces valeurs renseignent les champs de saisie correspondants dans le bac à sable API. Les utilisateurs peuvent remplacer les valeurs préremplies, et leurs modifications sont conservées dans le stockage local.

  Seules les valeurs correspondant au schéma de sécurité du point de terminaison actuel sont appliquées.

  <Expandable title="propriétés">
    <ParamField path="header" type="Record<string, unknown>">
      Valeurs d’en-tête à préremplir, indexées par nom d’en-tête.
    </ParamField>

    <ParamField path="query" type="Record<string, unknown>">
      Valeurs des paramètres de requête à préremplir, indexées par nom de paramètre.
    </ParamField>

    <ParamField path="cookie" type="Record<string, unknown>">
      Valeurs de cookie à préremplir, indexées par nom de cookie.
    </ParamField>

    <ParamField path="server" type="Record<string, string>">
      Valeurs des variables de serveur à préremplir, indexées par nom de variable.
    </ParamField>

    <ParamField path="path" type="Record<string, unknown>">
      Valeurs des paramètres de chemin à préremplir, indexées par nom de paramètre.
    </ParamField>
  </Expandable>
</ParamField>

<div id="feature-availability">
  ## Disponibilité des fonctionnalités
</div>

Certaines fonctionnalités se comportent différemment ou ne sont pas disponibles lorsque vous activez l’authentification. Mintlify ne permet pas de servir publiquement des fichiers arbitraires sur un site authentifié. Tous les fichiers hébergés, y compris `llms.txt`, `llms-full.txt` et `skill.md`, sont soumis aux mêmes exigences d’authentification que vos pages de documentation.

| Fonctionnalité                                             | Public                   | Entièrement authentifié (toutes les pages protégées)                                                 | Partiellement authentifié (certaines pages publiques)                                                       |
| :--------------------------------------------------------- | :----------------------- | :--------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------- |
| [llms.txt et llms-full.txt](/fr/ai/llmstxt)                   | Prise en charge complète | Disponibles après authentification, les outils d’IA peuvent donc ne pas pouvoir accéder aux fichiers | Disponibles après authentification, les outils d’IA peuvent donc ne pas pouvoir accéder aux fichiers        |
| [serveur MCP](/fr/ai/model-context-protocol)                  | Prise en charge complète | Nécessite une authentification pour se connecter                                                     | Disponible sans authentification pour les pages publiques et avec authentification pour les pages protégées |
| [export Markdown](/fr/ai/markdown-export)                     | Prise en charge complète | Prise en charge complète, respecte les groupes d’utilisateurs                                        | Prise en charge complète, respecte les groupes d’utilisateurs                                               |
| [export PDF](/fr/optimize/pdf-exports)                        | Prise en charge complète | Non pris en charge                                                                                   | Non pris en charge                                                                                          |
| [Recherche](/fr/assistant/index)                              | Prise en charge complète | Prise en charge complète, respecte les groupes d’utilisateurs                                        | Prise en charge complète, respecte les groupes d’utilisateurs                                               |
| [Assistant](/fr/assistant/index)                              | Prise en charge complète | Prise en charge complète, respecte les groupes d’utilisateurs                                        | Prise en charge complète, respecte les groupes d’utilisateurs                                               |
| [skill.md](/fr/ai/skillmd)                                    | Prise en charge complète | Non pris en charge                                                                                   | Non pris en charge                                                                                          |
| [Plan du site](/fr/optimize/seo#sitemaps-and-robotstxt-files) | Prise en charge complète | Disponible après authentification, mais exclut les pages des groupes                                 | Disponible après authentification, mais exclut les pages des groupes                                        |
| [robots.txt](/fr/optimize/seo#sitemaps-and-robotstxt-files)   | Prise en charge complète | Disponible après authentification                                                                    | Disponible après authentification                                                                           |