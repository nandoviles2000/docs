<ResponseField name="icon" type="string">
  要显示的图标。

  可选项：

  * [Font Awesome](https://fontawesome.com/icons) 图标名称，前提是你已在 `docs.json` 中将 `icons.library` [属性](/zh/organize/settings-appearance#param-icons) 设为 `fontawesome`
  * [Lucide](https://lucide.dev/icons) 图标名称，前提是你已在 `docs.json` 中将 `icons.library` [属性](/zh/organize/settings-appearance#param-icons) 设为 `lucide`
  * [Tabler](https://tabler.io/icons) 图标名称，前提是你已在 `docs.json` 中将 `icons.library` [属性](/zh/organize/settings-appearance#param-icons) 设为 `tabler`
  * 外部托管图标的 URL
  * 项目中图标文件的路径
  * 用花括号括起来的 SVG 代码

  对于自定义 SVG 图标：

  1. 使用 [SVGR converter](https://react-svgr.com/playground/) 转换 SVG。
  2. 将 SVG 代码粘贴到 SVG 输入框中。
  3. 从 JSX 输出框中复制完整的 `<svg>...</svg>` 元素。
  4. 用花括号将兼容 JSX 的 SVG 代码括起来：`icon={<svg ...> ... </svg>}`。
  5. 根据需要调整 `height` 和 `width`。
</ResponseField>

<ResponseField name="iconType" type="string">
  [Font Awesome](https://fontawesome.com/icons) 图标样式。仅用于 Font Awesome 图标。

  可选项：`regular`、`solid`、`light`、`thin`、`sharp-solid`、`duotone`、`brands`。
</ResponseField>