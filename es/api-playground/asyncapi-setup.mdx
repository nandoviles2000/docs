---
title: "Configuración de AsyncAPI"
description: "Configura documentación en tiempo real para WebSocket con archivos de especificación AsyncAPI para generar páginas interactivas de referencia de canales y mensajes."
keywords: ["asyncapi", "websocket"]
---

<div id="demo">
  ## Demostración
</div>

Consulta el [entorno de pruebas de WebSocket](/es/api-playground/websocket-playground) para ver un ejemplo del entorno de pruebas de AsyncAPI.

<div id="add-an-asyncapi-specification-file">
  ## Añade un archivo de especificación AsyncAPI
</div>

Para crear páginas para tus canales de WebSocket, debes contar con un documento de esquema de AsyncAPI válido en formato JSON o YAML que cumpla con la [especificación AsyncAPI 3.0](https://www.asyncapi.com/docs/reference/specification/v3.0.0).

<Tip>
  Usa [AsyncAPI Studio](https://studio.asyncapi.com/) para validar tu esquema de AsyncAPI.
</Tip>

```json {3}
/your-project
  |- docs.json
  |- asyncapi.json
```

<div id="auto-populate-websocket-pages">
  ## Generar automáticamente páginas de WebSocket
</div>

Para generar automáticamente páginas para todos los canales de tu esquema AsyncAPI, agrega una propiedad `asyncapi` a cualquier elemento de navegación. La propiedad `asyncapi` admite una ruta a un documento de esquema AsyncAPI en tu repositorio de documentación, una URL de un documento AsyncAPI alojado o una matriz de enlaces a documentos de esquema AsyncAPI.

<div id="examples-with-tabs">
  ### Ejemplos con pestañas
</div>

<CodeGroup>
  ```json Archivo local
  "navigation": {
    "tabs": [
      {
        "tab": "Referencia de API",
        "asyncapi": "/path/to/asyncapi.json"
      }
    ]
  }
  ```

  ```json URL remota
  "navigation": {
    "tabs": [
      {
        "tab": "Referencia de API",
        "asyncapi": "https://github.com/asyncapi/spec/blob/master/examples/simple-asyncapi.yml"
      }
    ]
  }
  ```

  ```json Varios archivos
  "navigation": {
    "tabs": [
      {
        "tab": "Referencia de API",
        "asyncapi": [
          "/path/to/events.json",
          "/path/to/webhooks.json"
        ]
      }
    ]
  }
  ```
</CodeGroup>

<Note>
  Cuando especificas varios archivos AsyncAPI, cada archivo genera su propio conjunto de páginas de canales.
</Note>

<div id="examples-with-groups">
  ### Ejemplos con grupos
</div>

```json
"navigation": {
  "tabs": [
    {
      "tab": "AsyncAPI",
      "groups": [
        {
          "group": "Websockets",
          "asyncapi": {
            "source": "/path/to/asyncapi.json",
            "directory": "websockets"
          }
        }
      ]
    }
  ]
}
```

<Note>
  El campo `directory` es opcional. Si no se especifica, Mintlify añade los archivos a la carpeta **api-reference** del repositorio de la documentación.
</Note>

<div id="channel-page">
  ## Página del canal
</div>

Si quieres tener más control sobre el orden de tus canales o hacer referencia solo a canales específicos, crea un archivo MDX con la propiedad `asyncapi` en el frontmatter.

```mdx
---
title: "Websocket Channel"
asyncapi: "/path/to/asyncapi.json channelName"
---
```