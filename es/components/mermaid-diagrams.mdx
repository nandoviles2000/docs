---
title: "Mermaid"
description: "Crea diagramas de flujo, diagramas de secuencia y otras visualizaciones en tu documentación con sintaxis Mermaid y renderizado automático."
keywords: ["Mermaid", "diagramas de flujo", "diagramas"]
---

[Mermaid](https://mermaid.js.org/) te permite crear diagramas de flujo, diagramas de secuencia, diagramas de Gantt y otros tipos de diagramas mediante texto y código.

Para ver la lista completa de tipos de diagramas compatibles y su sintaxis, consulta la [documentación de Mermaid](https://mermaid.js.org/intro/).

```mermaid placement="top-right"
  flowchart LR
    subgraph subgrafo1
        direction TB
        top1[arriba] --> bottom1[abajo]
    end
    subgraph subgrafo2
        direction TB
        top2[arriba] --> bottom2[abajo]
    end
    %% ^ These subgraphs are identical, except for the links to them:

    %% Link *to* subgraph1: subgraph1 direction is maintained
    outside --> subgraph1
    %% Link *within* subgraph2:
    %% subgraph2 inherits the direction of the top-level graph (LR)
    outside ---> top2
```

````mdx Mermaid flowchart example
```mermaid placement="top-right"
  flowchart LR
    subgraph subgraph1
        direction TB
        top1[top] --> bottom1[bottom]
    end
    subgraph subgraph2
        direction TB
        top2[top] --> bottom2[bottom]
    end
    %% ^ Estos subgrafos son idénticos, excepto por los enlaces hacia ellos:

    %% Enlace *hacia* subgraph1: se mantiene la dirección de subgraph1
    outside --> subgraph1
    %% Enlace *dentro de* subgraph2:
    %% subgraph2 hereda la dirección del grafo de nivel superior (LR)
    outside ---> top2
```
````

<div id="elk-layout-support">
  ## Compatibilidad con la disposición ELK
</div>

Mintlify admite el motor de disposición [ELK (Eclipse Layout Kernel)](https://www.eclipse.org/elk/) para diagramas Mermaid. ELK optimiza la organización para reducir las superposiciones y mejorar la legibilidad, lo que resulta útil para diagramas grandes o complejos.

Para usar la disposición ELK en tus diagramas Mermaid, agrega la directiva `%%{init: {'flowchart': {'defaultRenderer': 'elk'}}}%%` al principio de tu diagrama:

````mdx ELK layout example
```mermaid
%%{init: {'flowchart': {'defaultRenderer': 'elk'}}}%%
flowchart LR
    A[Start] --> B[Process 1]
    B --> C[Process 2]
    B --> D[Process 3]
    C --> E[End]
    D --> E
```
````

<div id="interactive-controls">
  ## Controles interactivos
</div>

Todos los diagramas de Mermaid incluyen controles interactivos de zoom y desplazamiento. De forma predeterminada, los controles aparecen cuando la altura del diagrama supera los 120px.

* **Acercar/alejar**: Usa los botones de zoom para aumentar o reducir la escala del diagrama.
* **Desplazamiento**: Usa las flechas de dirección para moverte por el diagrama.
* **Restablecer la vista**: Haz clic en el botón de restablecimiento para volver a la vista original.

Los controles son especialmente útiles en diagramas grandes o complejos que no caben por completo en el área visible.

<div id="properties">
  ## Propiedades
</div>

<ResponseField name="actions" type="boolean">
  Muestra u oculta los controles interactivos. Si se establece, anula el comportamiento predeterminado (los controles se muestran cuando la altura del diagrama supera los 120 px).
</ResponseField>

<ResponseField name="placement" type="string" default="bottom-right">
  Posición de los controles interactivos. Opciones: `top-left`, `top-right`, `bottom-left`, `bottom-right`.
</ResponseField>

<div id="examples">
  ### Ejemplos
</div>

Ocultar los controles de un diagrama:

````mdx
```mermaid actions={false}
flowchart LR
    A --> B
```
````

Muestra los controles en la esquina superior izquierda:

````mdx
```mermaid placement="top-left"
flowchart LR
    A --> B
```
````

Combina ambas propiedades:

````mdx
```mermaid placement="top-left" actions={true}
flowchart LR
    A --> B
```
````

<div id="syntax">
  ## Sintaxis
</div>

Para crear un diagrama de Mermaid, escribe la definición del diagrama dentro de un bloque de código de Mermaid.

````mdx
```mermaid
// El código de tu diagrama Mermaid va aquí
```
````