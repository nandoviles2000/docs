---
title: "Campos de respuesta"
description: "Documenta los campos de respuesta de la API con el componente ResponseField mediante anotaciones de tipo, descripciones e indicadores de campo obligatorio u opcional."
keywords: ["salida de la API", "valores devueltos", "estructura de la respuesta"]
---

Usa el componente `<ResponseField>` para definir los valores devueltos de una API. En muchas páginas de documentación, también se usa `<ResponseField>` cuando necesitas enumerar los tipos de algún elemento.

<ResponseField name="response" type="string" required>
  Ejemplo de campo de respuesta
</ResponseField>

```mdx
<ResponseField name="response" type="string" required>
  A response field example
</ResponseField>
```

<div id="props">
  ## Propiedades
</div>

<ResponseField name="name" type="string" required>
  El nombre del valor de respuesta.
</ResponseField>

<ResponseField name="type" type="string" required>
  El tipo esperado del valor de respuesta; puede ser cualquier cadena.
</ResponseField>

<ResponseField name="default" type="string">
  El valor predeterminado.
</ResponseField>

<ResponseField name="required" type="boolean">
  Muestra &quot;required&quot; junto al nombre del campo.
</ResponseField>

<ResponseField name="deprecated" type="boolean">
  Indica si un campo está obsoleto.
</ResponseField>

<ResponseField name="pre" type="string[]">
  Etiquetas que aparecen antes del nombre del campo
</ResponseField>

<ResponseField name="post" type="string[]">
  Etiquetas que aparecen después del nombre del campo
</ResponseField>

<RequestExample>
  ```mdx Ejemplo de campo de respuesta
  <ResponseField name="response" type="string" required>
    Un ejemplo de campo de respuesta
  </ResponseField>
  ```
</RequestExample>