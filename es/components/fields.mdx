---
title: "Campos"
description: "Usa los componentes ParamField y ResponseField para documentar los parámetros de solicitud y respuesta de la API con tipos, valores predeterminados y reglas de validación."
keywords: ["ParamField", "ResponseField", "parámetros de solicitud", "documentación de respuestas"]
---

Usa campos para documentar los parámetros y las respuestas de la API. Hay dos tipos de campos: los campos de parámetros y los campos de respuesta.

<div id="parameter-field">
  ## Campo de parámetro
</div>

Usa el componente `<ParamField>` para definir parámetros de tus API o SDK. Al agregar un `ParamField`, se añade automáticamente un [entorno de pruebas de API](/es/api-playground/overview).

<ParamField path="param" type="string" required>
  Un ejemplo de campo de parámetro
</ParamField>

```mdx
<ParamField path="param" type="string" required>
  An example of a parameter field
</ParamField>
```

<div id="properties">
  ### Propiedades
</div>

<ParamField body="query, path, body, or header" type="string">
  Indica si el parámetro es de tipo query, path, body o header. Va seguido del
  nombre del parámetro.
</ParamField>

<ParamField body="type" type="string">
  Tipo esperado del valor del parámetro.

  Admite `number`, `string`, `boolean`, `object`.

  Define arrays con el sufijo `[]`. Por ejemplo, `string[]`.
</ParamField>

<ParamField body="required" type="boolean">
  Indica si el parámetro es obligatorio.
</ParamField>

<ParamField body="deprecated" type="boolean">
  Indica si el parámetro está obsoleto.
</ParamField>

<ParamField body="default" type="any">
  Valor predeterminado que se rellena cuando el valor de la solicitud está vacío
</ParamField>

<ParamField body="placeholder" type="string">
  Texto de marcador de posición para la entrada en el entorno de pruebas.
</ParamField>

<ParamField body="children" type="string">
  Descripción del parámetro (compatible con Markdown).
</ParamField>

<div id="response-field">
  ## Campo de respuesta
</div>

El componente `<ResponseField>` define los valores de retorno de una API.

<ResponseField name="response" type="string" required>
  Un ejemplo de campo de respuesta
</ResponseField>

```mdx
<ResponseField name="response" type="string" required>
  A response field example
</ResponseField>
```

<div id="properties">
  ### Propiedades
</div>

<ResponseField name="name" type="string" required>
  El nombre del valor de respuesta.
</ResponseField>

<ResponseField name="type" type="string" required>
  El tipo esperado del valor de respuesta. Puede ser cualquier cadena.
</ResponseField>

<ResponseField name="default" type="string">
  El valor predeterminado.
</ResponseField>

<ResponseField name="required" type="boolean">
  Indica si el valor de respuesta es obligatorio.
</ResponseField>

<ResponseField name="deprecated" type="boolean">
  Indica si un campo está obsoleto.
</ResponseField>

<ResponseField name="pre" type="string[]">
  Etiquetas que aparecen antes del nombre del campo.
</ResponseField>

<ResponseField name="post" type="string[]">
  Etiquetas que aparecen después del nombre del campo.
</ResponseField>