---
title: "Tabs"
description: "Usa el componente Tabs para organizar el contenido en paneles intercambiables y mostrar distintas opciones, versiones de plataforma o ejemplos de idiomas."
keywords: ["contenido en pestañas", "paneles de contenido", "contenido intercambiable"]
---

Usa pestañas para organizar el contenido en varios paneles entre los que los usuarios pueden alternar. Puedes añadir tantas pestañas como quieras e incluir otros componentes dentro de cada una.

<Tabs>
  <Tab title="Primera pestaña">
    ☝️ Te damos la bienvenida al contenido que solo puedes ver dentro de la primera pestaña.

    Puedes añadir cualquier cantidad de componentes dentro de las pestañas. Por ejemplo, un bloque de código:

    ```java HelloWorld.java
      class HelloWorld {
          public static void main(String[] args) {
              System.out.println("Hello, World!");
          }
      }
    ```
  </Tab>

  <Tab title="Segunda pestaña" icon="leaf">
    ✌️ Aquí tienes contenido que solo está dentro de la segunda pestaña.

    ¡Esta tiene un icono de <Icon icon="leaf" />!
  </Tab>

  <Tab title="Tercera pestaña">
    💪 Aquí tienes contenido que solo está dentro de la tercera pestaña.
  </Tab>
</Tabs>

````mdx Tabs example
<Tabs>
  <Tab title="First tab">
    ☝️ Welcome to the content that you can only see inside the first tab.

    You can add any number of components inside of tabs. For example, a code block:
    ```java HelloWorld.java
      class HelloWorld {
          public static void main(String[] args) {
              System.out.println("Hello, World!");
          }
      }
    ```
  </Tab>
  <Tab title="Second tab" icon="leaf">
    ✌️ Here's content that's only inside the second tab.

    This one has a <Icon icon="leaf" /> icon!
  </Tab>
  <Tab title="Third tab">
    💪 Here's content that's only inside the third tab.
  </Tab>
</Tabs>
````

Las pestañas con títulos coincidentes se mantienen sincronizadas en toda la página. Por ejemplo, si tienes varios grupos de pestañas que incluyen una pestaña con el título `JavaScript`, al seleccionar `JavaScript` en un grupo de pestañas, se seleccionará automáticamente `JavaScript` en los demás. Esto ayuda a que los usuarios que eligen un lenguaje o framework una sola vez vean esa elección reflejada en todas partes. Las pestañas también se sincronizan con los [grupos de código](/es/components/code-groups) que tienen títulos coincidentes.

Para desactivar la sincronización de pestañas, agrega `sync={false}` a un componente `<Tabs>`.

```mdx Disable tab sync example
<Tabs sync={false}>
  <Tab title="First tab">
    Este grupo de pestañas funciona de forma independiente.
  </Tab>
  <Tab title="Second tab">
    Seleccionar pestañas aquí no afectará a otros grupos de pestañas.
  </Tab>
</Tabs>
```

<div id="tabs-properties">
  ## Propiedades de Tabs
</div>

Configura estas propiedades en el componente contenedor `<Tabs>`.

<ResponseField name="defaultTabIndex" type="number" default="0">
  El índice de la pestaña que se mostrará de forma predeterminada (empezando en 0). Por ejemplo, establécelo en `1` para mostrar la segunda pestaña al cargar la página.
</ResponseField>

<ResponseField name="sync" type="boolean" default="true">
  Cuando es `true`, las pestañas se sincronizan con otras pestañas y grupos de código de la página que tengan títulos coincidentes. Establécelo en `false` para que las pestañas sean independientes.
</ResponseField>

<ResponseField name="borderBottom" type="boolean">
  Añade un borde inferior y espaciado interno al contenedor de pestañas. Resulta útil para separar visualmente el contenido en pestañas del resto de la página, especialmente cuando las pestañas contienen contenido de distinta longitud.
</ResponseField>

<div id="tab-properties">
  ## Propiedades de Tab
</div>

Configura estas propiedades en cada componente `<Tab>`.

<ResponseField name="title" type="string" required>
  El título de la pestaña. Los títulos cortos facilitan la navegación. Las pestañas con el mismo título sincronizan su selección.
</ResponseField>

<ResponseField name="id" type="string">
  Un ID personalizado para la pestaña que se usa en enlaces de anclaje. Si no se proporciona, usa el mismo valor que `title`.
</ResponseField>

<ResponseField name="icon" type="string">
  Un icono de [Font Awesome](https://fontawesome.com/icons), un icono de [Lucide](https://lucide.dev/icons), una URL de un icono o la ruta relativa de un icono.
</ResponseField>

<ResponseField name="iconType" type="string">
  Solo para iconos de Font Awesome: uno de `regular`, `solid`, `light`, `thin`, `sharp-solid`, `duotone`, `brands`.
</ResponseField>