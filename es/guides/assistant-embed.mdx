---
title: "Tutorial: Crea un asistente de documentación en la aplicación"
sidebarTitle: "Crea un asistente en la aplicación"
description: "Crea e integra un asistente de documentación en la aplicación que responda a las preguntas de los usuarios con información citada de tu sitio de documentación de Mintlify."
keywords: ["asistente integrado", "chat en la aplicación", "widget", "chatbot"]
---

<div id="what-you-will-build">
  ## Lo que vas a crear
</div>

Un widget reutilizable que integra el [asistente](/es/assistant/index) directamente en tu aplicación. El widget ofrece:

* Un botón flotante que abre un panel de chat al hacer clic
* Respuestas en tiempo real basadas en la información de tu documentación
* Representación de mensajes con compatibilidad con Markdown

Los usuarios pueden usar el widget para obtener ayuda con tu producto sin salir de tu aplicación.

<Frame>
  <img src="/images/assistant/assistant-embed-demo.gif" alt="Demostración de la apertura del widget del asistente y de un usuario escribiendo «¿Cómo empiezo?». Luego, el asistente responde." />
</Frame>

<div id="prerequisites">
  ## Requisitos previos
</div>

* [Plan Pro o Enterprise de Mintlify](https://mintlify.com/pricing)
* Tu nombre de dominio, que aparece al final de la URL de tu panel. Por ejemplo, si la URL de tu panel es `https://dashboard.mintlify.com/org-name/domain-name`, tu nombre de dominio es `domain-name`
* Una [clave de API del asistente](https://dashboard.mintlify.com/settings/organization/api-keys)
* Node.js v18 o una versión superior, y npm instalados
* Conocimientos básicos de React

<div id="get-your-assistant-api-key">
  ### Obtén tu clave de API del asistente
</div>

1. Ve a la página de [claves de API](https://dashboard.mintlify.com/settings/organization/api-keys) en tu panel.
2. Haz clic en **Create Assistant API Key**.
3. Copia la clave de API del asistente (empieza por `mint_dsc_`) y guárdala en un lugar seguro.

<Note>
  La clave de API del asistente es un token público que puedes usar en el código del frontend. Las llamadas que usan este token cuentan para el límite de mensajes de tu plan y pueden generar cargos por excederlo.
</Note>

<div id="set-up-the-example">
  ## Configura el ejemplo
</div>

Clona el [repositorio de ejemplo](https://github.com/mintlify/assistant-embed-example) y adáptalo a tus necesidades.

<Steps>
  <Step title="Clona el repositorio">
    ```bash
    git clone https://github.com/mintlify/assistant-embed-example.git
    cd assistant-embed-example
    ```
  </Step>

  <Step title="Elige tu herramienta de desarrollo">
    El repositorio incluye ejemplos de Next.js y Vite. Elige la herramienta que prefieras.

    <CodeGroup>
      ```bash title="Next.js"
      cd nextjs
      npm install
      ```

      ```bash title="Vite"
      cd vite
      npm install
      ```
    </CodeGroup>
  </Step>

  <Step title="Configura tu proyecto">
    Abre `src/config.js` y actualízalo con los datos de tu proyecto de Mintlify.

    ```js src/config.js
    export const ASSISTANT_CONFIG = {
      domain: 'your-domain',
      docsURL: 'https://yourdocs.mintlify.app',
    };
    ```

    Reemplaza:

    * `your-domain` por el dominio de tu proyecto de Mintlify, que encontrarás al final de la URL de tu panel.
    * `https://yourdocs.mintlify.app` por la URL real de tu documentación.
  </Step>

  <Step title="Agrega tu token de API">
    Crea un archivo `.env` en la raíz del proyecto.

    ```bash .env
    VITE_MINTLIFY_TOKEN=mint_dsc_your_token_here
    ```

    Reemplaza `mint_dsc_your_token_here` por tu clave de API del asistente.
  </Step>

  <Step title="Inicia el servidor de desarrollo">
    ```bash
    npm run dev
    ```

    Abre tu aplicación en el navegador y haz clic en el botón **Ask** para abrir el widget del asistente.
  </Step>
</Steps>

<div id="customization-ideas">
  ## Ideas de personalización
</div>

<div id="source-citations">
  ### Citas de fuentes
</div>

Extrae y muestra las fuentes de las respuestas del asistente:

```jsx
const extractSources = (parts) => {
  return parts
    ?.filter(p => p.type === 'tool-invocation' && p.toolInvocation?.toolName === 'search')
    .flatMap(p => p.toolInvocation?.result || [])
    .map(source => ({
      url: source.url || source.path,
      title: source.metadata?.title || source.path,
    })) || [];
};

// En el renderizado de tus mensajes:
{messages.map((message) => {
  const sources = message.role === 'assistant' ? extractSources(message.parts) : [];
  return (
    <div key={message.id}>
      {/* contenido del mensaje */}
      {sources.length > 0 && (
        <div className="mt-2 text-xs">
          <p className="font-semibold">Sources:</p>
          {sources.map((s, i) => (
            <a key={i} href={s.url} target="_blank" rel="noopener noreferrer" className="text-blue-600">
              {s.title}
            </a>
          ))}
        </div>
      )}
    </div>
  );
})}
```

<div id="track-conversation-threads">
  ### Seguimiento de hilos de conversación
</div>

Almacena los ID y las claves de los hilos para mantener el historial de la conversación entre sesiones.

Cuando un usuario crea un nuevo hilo de conversación, el servidor devuelve dos valores en los encabezados de la respuesta:

* `X-Thread-Id`: El identificador del hilo
* `X-Thread-Key`: Una clave secreta del hilo (solo se devuelve una vez, cuando se crea el hilo)

Debes capturar y conservar ambos valores en la primera respuesta. En cada mensaje posterior, incluye `threadId` y `threadKey` en el cuerpo de la solicitud. Si envías un `threadId` sin el `threadKey` correspondiente, el servidor devuelve un error `404`.

<Warning>
  Debes almacenar el encabezado `X-Thread-Key` de inmediato. El servidor solo lo devuelve cuando se crea un hilo nuevo. No puedes recuperarlo después.
</Warning>

```jsx
import { useState, useEffect } from 'react';

export function AssistantWidget({ domain, docsURL }) {
  const [threadId, setThreadId] = useState(null);
  const [threadKey, setThreadKey] = useState(null);

  useEffect(() => {
    // Recuperar el ID de hilo y la clave guardados en localStorage
    const savedId = localStorage.getItem('assistant-thread-id');
    const savedKey = localStorage.getItem('assistant-thread-key');
    if (savedId && savedKey) {
      setThreadId(savedId);
      setThreadKey(savedKey);
    }
  }, []);

  const { messages, input, handleInputChange, handleSubmit, isLoading } = useChat({
    api: `https://api.mintlify.com/discovery/v1/assistant/${domain}/message`,
    headers: {
      'Authorization': `Bearer ${import.meta.env.VITE_MINTLIFY_TOKEN}`,
    },
    body: {
      fp: 'anonymous',
      retrievalPageSize: 5,
      ...(threadId && { threadId }),
      ...(threadKey && { threadKey }),
    },
    streamProtocol: 'data',
    sendExtraMessageFields: true,
    fetch: async (url, options) => {
      const response = await fetch(url, options);
      const newThreadId = response.headers.get('x-thread-id');
      const newThreadKey = response.headers.get('x-thread-key');
      if (newThreadId) {
        setThreadId(newThreadId);
        localStorage.setItem('assistant-thread-id', newThreadId);
      }
      if (newThreadKey) {
        setThreadKey(newThreadKey);
        localStorage.setItem('assistant-thread-key', newThreadKey);
      }
      return response;
    },
  });

  // ... resto del componente
}
```

<div id="add-keyboard-shortcuts">
  ### Añade atajos de teclado
</div>

Permite que los usuarios abran el widget y envíen mensajes mediante atajos de teclado:

```jsx
useEffect(() => {
  const handleKeyDown = (e) => {
    // Cmd/Ctrl + Shift + I para mostrar/ocultar el widget
    if ((e.metaKey || e.ctrlKey) && e.shiftKey && e.key === 'I') {
      e.preventDefault();
      setIsOpen((prev) => !prev);
    }

    // Enter (cuando el widget tiene el foco) para enviar
    if (e.key === 'Enter' && !e.shiftKey && document.activeElement.id === 'assistant-input') {
      e.preventDefault();
      handleSubmit();
    }
  };

  window.addEventListener('keydown', handleKeyDown);
  return () => window.removeEventListener('keydown', handleKeyDown);
}, [handleSubmit]);
```