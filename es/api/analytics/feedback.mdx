---
title: "Obtener comentarios"
description: "Recupera los comentarios de los usuarios de tu documentación,
  incluidas las valoraciones de las páginas y los comentarios sobre los
  fragmentos de código."
openapi: /es/analytics.openapi.json GET /v1/analytics/{projectId}/feedback
keywords: [ "analítica", "comentarios", "exportación", "comentarios de los usuarios" ]
---

<div id="usage">
  ## Uso
</div>

Usa este endpoint para exportar los comentarios de los usuarios recopilados en tu documentación. Los comentarios incluyen comentarios contextuales de las valoraciones de páginas, comentarios sobre fragmentos de código y comentarios de agentes.

Pagina los resultados con el parámetro `cursor` que se devuelve en la respuesta. Sigue recuperando resultados mientras `hasMore` sea `true`.

<div id="filtering">
  ## Filtrado
</div>

Filtra los comentarios según:

* **Rango de fechas**: usa `dateFrom` y `dateTo` para limitar los resultados a un período específico
* **Origen**: filtra por tipos de comentarios como `code_snippet`, `contextual` o `agent`
* **Estado**: filtra por valores de estado como `pending`, `in_progress`, `resolved` o `dismissed`

<div id="response-types">
  ## Tipos de respuesta
</div>

La respuesta contiene distintos tipos de comentarios según la fuente:

* **Comentarios contextuales**: Incluyen el valor booleano `helpful` y el correo electrónico opcional `contact`
* **Comentarios sobre fragmentos de código**: Incluyen los campos `code`, `filename` y `lang`
* **Comentarios del agente**: Incluyen un campo `comment` con el texto de los comentarios del agente y la `path` de la página a la que se refiere