---
title: "Obtener conversaciones del asistente"
description: "Recupera el historial de conversaciones del asistente de IA,
  incluidas las consultas, las respuestas, las fuentes citadas y las
  categorías."
openapi: /es/analytics.openapi.json GET /v1/analytics/{projectId}/assistant
keywords: [ "analítica", "asistente", "conversaciones", "exportación", "IA" ]
---

<div id="usage">
  ## Uso
</div>

Usa este endpoint para exportar el historial de conversaciones del asistente de IA de tu documentación. Cada conversación incluye la consulta del usuario, la respuesta del asistente, las fuentes citadas y la categoría de la consulta.

Recorre los resultados paginados usando el parámetro `cursor` que se devuelve en la respuesta. Sigue recuperando resultados mientras `hasMore` sea `true`.

<div id="filtering">
  ## Filtrado
</div>

Filtra las conversaciones por intervalo de fechas con los parámetros `dateFrom` y `dateTo`.

<div id="conversation-data">
  ## Datos de la conversación
</div>

Cada conversación incluye:

* **query**: La pregunta del usuario
* **response**: La respuesta del asistente
* **sources**: Páginas referenciadas en la respuesta, con título y URL
* **queryCategory**: Clasificación del tipo de consulta (si está disponible)