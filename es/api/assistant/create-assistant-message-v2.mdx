---
title: "Crear mensaje de asistente"
description: "Envía mensajes al asistente de IA y recibe respuestas en streaming
  con AI SDK v5 o posterior mediante el hook useChat."
openapi: /es/discovery-openapi.json POST /v2/assistant/{domain}/message
keywords:
  [
    "mensaje de asistente",
    "insertar",
    "chat",
    "integrar",
    "v2",
    "ai sdk v5",
    "ai sdk v6"
  ]
---

<Info>
  El endpoint v2 de mensajes de asistente es compatible con **AI SDK v5+**. Si
  usas AI SDK v4, usa en su lugar el [endpoint v1 de mensajes de
  asistente](/es/api/assistant/create-assistant-message).
</Info>

<div id="integration-with-usechat">
  ## Integración con `useChat`
</div>

El hook `useChat` del SDK de IA de Vercel es la forma recomendada de integrar la API del asistente en tu aplicación.

<Steps>
  <Step title="Instalar AI SDK">
    ```bash
    npm i ai@^6 @ai-sdk/react
    ```
  </Step>

  <Step title="Usar el hook">
    ```tsx
    import { useState } from "react";
    import { useChat } from "@ai-sdk/react";
    import { DefaultChatTransport } from "ai";

    function MyComponent({ domain }) {
      const [input, setInput] = useState("");

      const { messages, sendMessage } = useChat({
        transport: new DefaultChatTransport({
          api: `https://api.mintlify.com/discovery/v2/assistant/${domain}/message`,
          headers: {
            Authorization: `Bearer ${process.env.PUBLIC_MINTLIFY_ASSISTANT_KEY}`,
          },
          body: {
            fp: "anonymous",
            retrievalPageSize: 5,
            context: [
              {
                type: "code",
                value: 'const example = "code snippet";',
                elementId: "code-block-1",
              },
            ],
          },
        }),
      });

      return (
        <div>
          {messages.map((message) => (
            <div key={message.id}>
              {message.role === "user" ? "Usuario: " : "Asistente: "}
              {message.parts
                .filter((part) => part.type === "text")
                .map((part) => part.text)
                .join("")}
            </div>
          ))}
          <form
            onSubmit={(e) => {
              e.preventDefault();
              if (input.trim()) {
                sendMessage({ text: input });
                setInput("");
              }
            }}
          >
            <input value={input} onChange={(e) => setInput(e.target.value)} />
            <button type="submit">Enviar</button>
          </form>
        </div>
      );
    }
    ```

    **Configuración obligatoria:**

    * `transport` - Usa `DefaultChatTransport` para configurar la conexión con la API.
    * `body.fp` - Identificador de huella digital (usa `'anonymous'` o un identificador de usuario único).
    * `body.retrievalPageSize` - Número de resultados de búsqueda que se usarán (recomendado: 5).

    **Configuración opcional:**

    * `body.context` - Lista de información contextual que se proporciona al asistente. Cada objeto de contexto contiene:
      * `type` - `'code'` o `'textSelection'`.
      * `value` - El fragmento de código o el contenido del texto seleccionado.
      * `path` (opcional) - Ruta al archivo o a la página de origen.
      * `elementId` (opcional) - Identificador del elemento de la interfaz que contiene el contexto.
    * `body.currentPath` - La ruta de la página que el usuario está viendo actualmente. Cuando se proporciona, el asistente usa este contexto para ofrecer respuestas más relevantes. Longitud máxima: 200 caracteres.
  </Step>
</Steps>

Consulta [useChat](https://ai-sdk.dev/docs/reference/ai-sdk-ui/use-chat) y [Transport](https://ai-sdk.dev/docs/ai-sdk-ui/transport) en la documentación del SDK de IA para obtener más detalles.

<div id="rate-limits">
  ## Límites de tasa
</div>

La API del asistente tiene los siguientes límites:

* 10,000 usos por clave al mes
* 10,000 solicitudes por organización de Mintlify por hora
* 10,000 solicitudes por IP al día