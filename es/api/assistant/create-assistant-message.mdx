---
title: "Crear mensaje del asistente (v1)"
description: "Obsoleto. Envía mensajes al asistente de IA con AI SDK v4. Usa el
  endpoint v2 para AI SDK v5 o versiones posteriores."
openapi: /es/discovery-openapi.json POST /v1/assistant/{domain}/message
tag: "Obsoleto"
keywords: [ "mensaje del asistente", "incrustar", "chat", "integrar", "ai sdk v4" ]
---

<Badge color="orange">Obsoleto</Badge>

<Info>
  El endpoint v1 para mensajes del asistente es compatible con **AI SDK v4**. Si usas AI SDK v5 o una versión posterior, usa en su lugar el [endpoint v2 para mensajes del asistente](/es/api/assistant/create-assistant-message-v2).
</Info>

<div id="integration-with-usechat">
  ## Integración con `useChat`
</div>

El hook `useChat` del AI SDK de Vercel es la forma recomendada de integrar la API del asistente en tu aplicación.

<Steps>
  <Step title="Instala AI SDK v4">
    ```bash
    npm i ai@^4.1.15
    ```
  </Step>

  <Step title="Usa el hook">
    ```tsx
    import { useChat } from 'ai/react';

    function MyComponent({ domain }) {
      const { messages, input, handleInputChange, handleSubmit, isLoading } = useChat({
        api: `https://api.mintlify.com/discovery/v1/assistant/${domain}/message`,
        headers: {
          'Authorization': `Bearer ${process.env.PUBLIC_MINTLIFY_ASSISTANT_KEY}`,
        },
        body: {
          fp: 'anonymous',
          retrievalPageSize: 5,
          context: [
            {
              type: 'code',
              value: 'const example = "code snippet";',
              elementId: 'code-block-1',
            },
          ],
        },
        streamProtocol: 'data',
        sendExtraMessageFields: true,
      });

      return (
        <div>
          {messages.map((message) => (
            <div key={message.id}>
              {message.role === 'user' ? 'User: ' : 'Assistant: '}
              {message.content}
            </div>
          ))}
          <form onSubmit={handleSubmit}>
            <input value={input} onChange={handleInputChange} />
            <button type="submit">Send</button>
          </form>
        </div>
      );
    }
    ```

    **Configuración obligatoria para Mintlify:**

    * `streamProtocol: 'data'` - Obligatorio para las respuestas en streaming.
    * `sendExtraMessageFields: true` - Obligatorio para enviar metadatos de mensajes.
    * `body.fp` - Identificador de huella digital (usa &#39;anonymous&#39; o un identificador de usuario).
    * `body.retrievalPageSize` - Número de resultados de Búsqueda que se usarán (recomendado: 5).

    **Configuración opcional:**

    * `body.context` - Matriz de información contextual que se proporciona al asistente. Cada objeto de contexto contiene:
      * `type` - `'code'` o `'textSelection'`.
      * `value` - El fragmento de código o el contenido del texto seleccionado.
      * `elementId` (opcional) - Identificador del elemento de la interfaz que contiene el contexto.
    * `body.currentPath` - Ruta de la página que el usuario está viendo en ese momento. Cuando se proporciona, el asistente usa este contexto para ofrecer respuestas más relevantes. Longitud máxima: 200 caracteres.
  </Step>
</Steps>

Consulta [useChat](https://ai-sdk.dev/docs/reference/ai-sdk-ui/use-chat) en la documentación del AI SDK para obtener más información.

<div id="rate-limits">
  ## Límites de uso
</div>

La API del asistente tiene los siguientes límites:

* 10.000 usos por clave al mes
* 10.000 solicitudes por organización de Mintlify por hora
* 10.000 solicitudes por IP al día