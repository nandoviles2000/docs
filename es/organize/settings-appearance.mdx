---
title: "Apariencia e identidad de marca"
description: "Configura el tema, los colores, el logo, el favicon, las fuentes y el fondo en docs.json para controlar la identidad visual de tu sitio de documentación."
keywords: ["tema", "colores", "logo", "favicon", "fuentes", "fondo", "apariencia", "identidad de marca", "modo oscuro", "iconos", "estilos", "docs.json"]
---

import IconsOptional from "/snippets/es/icons-optional.mdx";

Usa esta configuración de `docs.json` para controlar la identidad visual de tu sitio de documentación: el tema de diseño, los colores de la marca, el logo, la tipografía y el fondo.

<div id="settings">
  ## Configuración
</div>

### `theme` - <Badge color="red">obligatorio</Badge>

El tema de diseño de tu sitio.

Uno de los siguientes: `mint`, `maple`, `palm`, `willow`, `linden`, `almond`, `aspen`, `sequoia`, `luma`.

Consulta [Temas](/es/customize/themes) para ver vistas previas y más detalles.

***

### `name` - <Badge color="red">obligatorio</Badge>

**Tipo:** `string`

El nombre de tu proyecto, organización o producto. Aparece en el título de la pestaña del navegador y en otras partes de tu sitio.

***

### `colors` - <Badge color="red">obligatorio</Badge>

**Tipo:** `object`

Los colores utilizados en tu documentación. Los colores se ven de forma distinta según el tema. Si solo proporcionas un color primario, se aplicará a todos los elementos de color.

<ResponseField name="colors.primary" type="string" required>
  El color primario de tu documentación. Por lo general, se usa para destacar elementos en el modo claro, con cierta variación según el tema.

  Debe ser un código hexadecimal que comience con `#`. Ejemplo: `"#0D9373"`.
</ResponseField>

<ResponseField name="colors.light" type="string">
  El color utilizado para destacar elementos en el modo oscuro.

  Debe ser un código hexadecimal que comience con `#`.
</ResponseField>

<ResponseField name="colors.dark" type="string">
  El color utilizado para los botones y los estados al pasar el cursor, tanto en el modo claro como en el oscuro, con cierta variación según el tema.

  Debe ser un código hexadecimal que comience con `#`.
</ResponseField>

```json docs.json
"colors": {
  "primary": "#0D9373",
  "light": "#55D799",
  "dark": "#0D9373"
}
```

***

<div id="logo">
  ### `logo`
</div>

**Tipo:** `string` o `object`

El logo de tu sitio. Proporciona una sola ruta de imagen o imágenes separadas para los modos claro y oscuro.

<ResponseField name="logo.light" type="string" required>
  Ruta al archivo de tu logo para el modo claro. Incluye la extensión del archivo. Ejemplo: `/logo/light.svg`.
</ResponseField>

<ResponseField name="logo.dark" type="string" required>
  Ruta al archivo de tu logo para el modo oscuro. Incluye la extensión del archivo. Ejemplo: `/logo/dark.svg`.
</ResponseField>

<ResponseField name="logo.href" type="string (uri)">
  La URL a la que se redirige al hacer clic en el logo. Si no se proporciona, el logo enlaza a la primera página del idioma seleccionado actualmente para la [documentación internacionalizada](/es/guides/internationalization), o a tu página de inicio en sitios de un solo idioma. Ejemplo: `https://yoursite.com`.
</ResponseField>

```json docs.json
"logo": {
  "light": "/logo/light.svg",
  "dark": "/logo/dark.svg",
  "href": "https://yoursite.com"
}
```

***

<div id="favicon">
  ### `favicon`
</div>

**Tipo:** `string` o `object`

Ruta a tu archivo favicon, incluida la extensión del archivo. Se redimensiona automáticamente a los tamaños adecuados para favicon. Proporciona un único archivo o archivos separados para el modo claro y el modo oscuro.

<ResponseField name="favicon.light" type="string" required>
  Ruta a tu favicon para el modo claro. Incluye la extensión del archivo. Ejemplo: `/favicon.png`.
</ResponseField>

<ResponseField name="favicon.dark" type="string" required>
  Ruta a tu favicon para el modo oscuro. Incluye la extensión del archivo. Ejemplo: `/favicon-dark.png`.
</ResponseField>

```json docs.json
"favicon": "/favicon.svg"
```

***

<div id="appearance">
  ### `appearance`
</div>

**Tipo:** `object`

Configuración del modo claro/oscuro.

<ResponseField name="appearance.default" type="&#x22;system&#x22; | &#x22;light&#x22; | &#x22;dark&#x22;">
  Modo de color predeterminado. Elige `system` para que coincida con la configuración del sistema operativo del usuario, o `light` o `dark` para forzar un modo específico. El valor predeterminado es `system`.
</ResponseField>

<ResponseField name="appearance.strict" type="boolean">
  Cuando es `true`, oculta el selector de modo claro/oscuro para que los usuarios no puedan cambiar de modo. El valor predeterminado es `false`.
</ResponseField>

```json docs.json
"appearance": {
  "default": "dark",
  "strict": true
}
```

***

<div id="fonts">
  ### `fonts`
</div>

**Tipo:** `object`

Fuentes personalizadas para tu documentación. La fuente predeterminada varía según el tema. Admite [Google Fonts](https://fonts.google.com) y fuentes alojadas localmente.

<ResponseField name="fonts.family" type="string" required>
  Nombre de la familia tipográfica, como `"Inter"` o `"Open Sans"`. Admite nombres de familia de [Google Fonts](https://fonts.google.com); se cargan automáticamente sin necesidad de indicar `source`.
</ResponseField>

<ResponseField name="fonts.weight" type="number">
  Peso de la fuente, como `400` o `700`. Las fuentes variables admiten pesos fraccionarios, como `550`.
</ResponseField>

<ResponseField name="fonts.source" type="string (uri)">
  URL de una fuente alojada o ruta a un archivo de fuente local. No es necesario para Google Fonts.

  * Alojada: `https://example.com/fonts/MyFont.woff2`
  * Local: `/fonts/MyFont.woff2`
</ResponseField>

<ResponseField name="fonts.format" type="&#x22;woff&#x22; | &#x22;woff2&#x22;">
  Formato del archivo de fuente. Obligatorio cuando usas el campo `source`.
</ResponseField>

<ResponseField name="fonts.heading" type="object">
  Sobrescribe la configuración de fuente solo para los encabezados.

  Acepta los mismos campos `family`, `weight`, `source` y `format` que el objeto `fonts` de nivel superior.
</ResponseField>

<ResponseField name="fonts.body" type="object">
  Sobrescribe la configuración de fuente solo para el cuerpo del texto.

  Acepta los mismos campos `family`, `weight`, `source` y `format` que el objeto `fonts` de nivel superior.
</ResponseField>

```json docs.json
"fonts": {
  "family": "Inter",
  "heading": {
    "family": "Playfair Display"
  }
}
```

***

<div id="icons">
  ### `icons`
</div>

**tipo:** `object`

Configuración de la biblioteca de iconos. Solo puedes usar una biblioteca de iconos por proyecto. Todos los nombres de iconos de tu documentación deben provenir de la biblioteca seleccionada.

<ResponseField name="icons.library" type="&#x22;fontawesome&#x22; | &#x22;lucide&#x22; | &#x22;tabler&#x22;" required>
  Biblioteca de iconos que se usará en toda tu documentación. El valor predeterminado es `fontawesome`.

  <Note>
    Puedes especificar la URL de un icono alojado externamente o la ruta a un archivo de icono en tu proyecto para cualquier icono concreto, independientemente de la configuración de la biblioteca.
  </Note>
</ResponseField>

```json docs.json
"icons": {
  "library": "lucide"
}
```

***

<div id="background">
  ### `background`
</div>

**Tipo:** `object`

Configuración de la imagen, la decoración y los colores de fondo.

<ResponseField name="background.decoration" type="&#x22;gradient&#x22; | &#x22;grid&#x22; | &#x22;windows&#x22;">
  Un patrón de fondo decorativo para tu tema.
</ResponseField>

<ResponseField name="background.color" type="object">
  Colores de fondo personalizados para los modos claro y oscuro.

  <Expandable title="background.color">
    <ResponseField name="light" type="string">
      Color de fondo para el modo claro. Debe ser un código hexadecimal que empiece por `#`.
    </ResponseField>

    <ResponseField name="dark" type="string">
      Color de fondo para el modo oscuro. Debe ser un código hexadecimal que empiece por `#`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="background.image" type="string or object">
  Imagen de fondo para tu sitio. Proporciona una sola ruta o rutas independientes para los modos claro y oscuro.

  <Expandable title="background.image">
    <ResponseField name="light" type="string" required>
      Ruta de la imagen de fondo para el modo claro. Ejemplo: `/background.png`.
    </ResponseField>

    <ResponseField name="dark" type="string" required>
      Ruta de la imagen de fondo para el modo oscuro. Ejemplo: `/background-dark.png`.
    </ResponseField>
  </Expandable>
</ResponseField>

```json docs.json
"background": {
  "decoration": "gradient",
  "color": {
    "light": "#F8FAFC",
    "dark": "#0F172A"
  }
}
```

***

<div id="styling">
  ### `styling`
</div>

**Tipo:** `object`

Controles detallados del estilo visual.

<ResponseField name="styling.eyebrows" type="&#x22;section&#x22; | &#x22;breadcrumbs&#x22;">
  El estilo del encabezado superior de la página (la etiqueta que se muestra en la parte superior de la página). Elige `section` para mostrar el nombre de la sección o `breadcrumbs` para mostrar la ruta de navegación completa. El valor predeterminado es `section`.
</ResponseField>

<ResponseField name="styling.latex" type="boolean">
  Controla si se cargan las hojas de estilo de LaTeX. De forma predeterminada, Mintlify detecta automáticamente el uso de LaTeX en tu contenido y carga las hojas de estilo necesarias.

  * Establécelo en `true` para forzar la carga de las hojas de estilo de LaTeX cuando falle la detección automática.
  * Establécelo en `false` para evitar cargar las hojas de estilo de LaTeX y mejorar el rendimiento si no usas expresiones matemáticas.
</ResponseField>

<ResponseField name="styling.codeblocks" type="&#x22;system&#x22; | &#x22;dark&#x22; | string | object">
  Tema de los bloques de código. El valor predeterminado es `"system"`.

  * `"system"`: Coincide con el modo actual del sitio (claro u oscuro)
  * `"dark"`: Usa siempre el modo oscuro
  * Una cadena con el nombre de un tema de [Shiki](https://shiki.style/themes): aplica ese tema a todos los bloques de código
  * Un objeto con las claves `light` y `dark`: aplica temas de Shiki distintos para cada modo

  <Expandable title="objeto styling.codeblocks">
    <ResponseField name="theme" type="string">
      Un único tema de Shiki para ambos modos.

      ```json
      "styling": {
        "codeblocks": {
          "theme": "dracula"
        }
      }
      ```
    </ResponseField>

    <ResponseField name="theme" type="object">
      Temas de Shiki independientes para los modos claro y oscuro.

      <Expandable title="theme">
        <ResponseField name="light" type="string" required>
          Nombre del tema de Shiki para el modo claro.
        </ResponseField>

        <ResponseField name="dark" type="string" required>
          Nombre del tema de Shiki para el modo oscuro.
        </ResponseField>
      </Expandable>

      ```json
      "styling": {
        "codeblocks": {
          "theme": {
            "light": "github-light",
            "dark": "github-dark"
          }
        }
      }
      ```
    </ResponseField>

    <ResponseField name="languages" type="object">
      Configuración personalizada de lenguajes para los bloques de código.

      <Expandable title="languages">
        <ResponseField name="custom" type="array of string">
          Rutas a archivos JSON que describen lenguajes personalizados de Shiki. Úsalo para añadir resaltado de sintaxis para lenguajes que no están en el conjunto predeterminado de Shiki. Cada archivo debe seguir el [formato de gramática de TextMate](https://macromates.com/manual/en/language_grammars).

          ```json
          "styling": {
            "codeblocks": {
              "languages": {
                "custom": ["/languages/my-language.json"]
              }
            }
          }
          ```
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

***

<div id="thumbnails">
  ### `thumbnails`
</div>

**Tipo:** `object`

Personalización de miniaturas para redes sociales y vistas previas de página.

<ResponseField name="thumbnails.appearance" type="&#x22;light&#x22; | &#x22;dark&#x22;">
  Tema visual de las miniaturas. Si no se configura, las miniaturas usan la combinación de colores de tu sitio definida en `colors`.
</ResponseField>

<ResponseField name="thumbnails.background" type="string">
  Imagen de fondo de las miniaturas. Puede ser una ruta relativa o una URL absoluta.
</ResponseField>

<ResponseField name="thumbnails.fonts" type="object">
  Configuración de fuentes para las miniaturas. Solo admite nombres de familias de Google Fonts.

  <Expandable title="thumbnails.fonts">
    <ResponseField name="family" type="string" required>
      Nombre de la familia tipográfica, como `"Open Sans"` o `"Playfair Display"`. Admite nombres de familias de [Google Fonts](https://fonts.google.com).
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="example">
  ## Ejemplo
</div>

```json docs.json
{
  "$schema": "https://mintlify.com/docs.json",
  "theme": "maple",
  "name": "Example Co.",
  "colors": {
    "primary": "#3B82F6",
    "light": "#93C5FD",
    "dark": "#1D4ED8"
  },
  "logo": {
    "light": "/logo/light.svg",
    "dark": "/logo/dark.svg",
    "href": "https://example.com"
  },
  "favicon": "/favicon.svg",
  "appearance": {
    "default": "system"
  },
  "fonts": {
    "family": "Inter"
  },
  "icons": {
    "library": "lucide"
  },
  "background": {
    "decoration": "gradient"
  }
}
```