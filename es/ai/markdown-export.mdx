---
title: "Exportación en Markdown"
description: "Exporta versiones limpias en Markdown de las páginas de tu documentación para herramientas de IA, integraciones con LLM y flujos de trabajo automatizados de procesamiento de contenido."
keywords: ["markdown", "eficiencia de tokens", "procesamiento de IA"]
---

import { PreviewButton } from "/snippets/previewbutton.jsx"

Markdown proporciona texto estructurado que las herramientas de IA pueden procesar con más eficiencia que HTML, lo que se traduce en mayor precisión en las respuestas, tiempos de procesamiento más rápidos y un menor uso de tokens.

Mintlify genera automáticamente versiones en Markdown de las páginas optimizadas para herramientas de IA e integraciones externas.

<div id="md-url-extension">
  ## Extensión `.md` en la URL
</div>

Agrega `.md` a la URL de cualquier página para ver una versión en Markdown.

<PreviewButton href="https://mintlify.com/docs/ai/markdown-export.md">Abrir esta página en Markdown</PreviewButton>

<div id="accept-header">
  ## Cabecera Accept
</div>

Envía una solicitud con `Accept: text/markdown` o `Accept: text/plain` a cualquier URL de página para recibir la versión en Markdown en lugar de HTML. Esto es útil para herramientas de IA e integraciones que obtienen el contenido de la documentación de forma programática.

```bash
curl -H "Accept: text/markdown" https://mintlify.com/docs/ai/markdown-export
```

<div id="audience-specific-content">
  ## Contenido específico para cada audiencia
</div>

Usa el componente [visibility](/es/components/visibility) para personalizar el contenido para audiencias humanas y de IA.

El contenido incluido en `<Visibility for="humans">` aparece en la página web, pero no en la salida de Markdown. El contenido incluido en `<Visibility for="agents">` aparece en la salida de Markdown, pero no en la página web.

```mdx
<Visibility for="humans">
  Click the **Get started** button in the top-right corner to create your account.
</Visibility>

<Visibility for="agents">
  To create an account, call `POST /v1/accounts` with a valid email address.
</Visibility>
```

<div id="agent-feedback">
  ## Comentarios de agentes
</div>

Si habilitas los [comentarios de agentes](/es/optimize/feedback#agent-feedback), Mintlify añade un bloque `<AgentInstructions>` a la exportación en Markdown de cada página para indicar a los agentes cómo enviar comentarios sobre el contenido de la página.

Los agentes envían comentarios haciendo una solicitud al endpoint con la ruta de la página y el texto del comentario.

Usa los comentarios de agentes para mejorar tus páginas para que los agentes puedan señalar lo que consideren incorrecto, desactualizado o confuso.

<div id="keyboard-shortcut">
  ## Atajo de teclado
</div>

Pulsa <kbd>Command</kbd> + <kbd>C</kbd> (<kbd>Ctrl</kbd> + <kbd>C</kbd> en Windows) para copiar una página en formato Markdown al portapapeles.