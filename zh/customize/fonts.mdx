---
title: "字体"
description: "使用 Google Fonts 或自托管字体文件，自定义文档站点中标题、正文和代码块的排版。"
keywords: ["Google Fonts", "本地字体", "自定义排版"]
---

为整个站点设置自定义字体，或分别为标题和正文设置不同字体。您可以使用 Google Fonts、本地字体文件或外部托管的字体。默认字体会因主题不同而有所变化。

在 `docs.json` 中使用 [fonts 属性](/zh/organize/settings-appearance#fonts) 配置字体。

<div id="google-fonts">
  ## Google Fonts
</div>

当你在 `docs.json` 中指定字体族名称时，Mintlify 会自动加载 [Google Fonts](https://fonts.google.com)。

```json docs.json
"fonts": {
  "family": "Inter"
}
```

<div id="local-fonts">
  ## 本地字体
</div>

要使用本地字体，请将字体文件放在项目目录中，并在 `docs.json` 配置文件中引用这些字体。

<div id="setting-up-local-fonts">
  ### 设置本地字体
</div>

<Steps>
  <Step title="将字体文件添加到项目">
    例如，创建一个 `fonts` 目录，并将字体文件添加到其中：

    ```text
    your-project/
    ├── fonts/
    │   ├── InterDisplay-Regular.woff2
    │   └── InterDisplay-Bold.woff2
    ├── docs.json
    └── ...
    ```
  </Step>

  <Step title="在 docs.json 中配置字体">
    使用相对于项目根目录的路径来引用本地字体：

    ```json docs.json
    {
      "fonts": {
        "family": "InterDisplay",
        "source": "/fonts/InterDisplay-Regular.woff2",
        "format": "woff2",
        "weight": 400
      }
    }
    ```
  </Step>
</Steps>

<div id="local-fonts-for-headings-and-body">
  ### 标题和正文的本地字体
</div>

在 `docs.json` 中分别为标题和正文字体配置本地字体：

```json docs.json
{
  "fonts": {
    "heading": {
      "family": "InterDisplay",
      "source": "/fonts/InterDisplay-Bold.woff2",
      "format": "woff2",
      "weight": 700
    },
    "body": {
      "family": "InterDisplay",
      "source": "/fonts/InterDisplay-Regular.woff2",
      "format": "woff2",
      "weight": 400
    }
  }
}
```

<div id="externally-hosted-fonts">
  ## 外部托管的字体
</div>

在 `docs.json` 中引用字体源 URL，即可使用外部托管的字体：

```json docs.json
{
  "fonts": {
    "family": "Hubot Sans",
    "source": "https://mintlify-assets.b-cdn.net/fonts/Hubot-Sans.woff2",
    "format": "woff2",
    "weight": 400
  }
}
```

<div id="font-configuration-reference">
  ## 字体配置参考
</div>

<ResponseField name="fonts" type="object">
  文档的字体配置。

  <Expandable title="字体">
    <ResponseField name="family" type="string" required>
      字体族名称，例如 “Inter” 或 “Playfair Display”。
    </ResponseField>

    <ResponseField name="weight" type="number">
      字体粗细，例如 400 或 700。可变字体支持 550 这样的精确字重。
    </ResponseField>

    <ResponseField name="source" type="string (uri)">
      字体来源的 URL，例如 `https://mintlify-assets.b-cdn.net/fonts/Hubot-Sans.woff2`，或本地字体文件路径，例如 `/assets/fonts/InterDisplay.woff2`。如果指定的是 Google Font 的 `family` 名称，系统会自动加载 Google Fonts，因此无需提供来源 URL。
    </ResponseField>

    <ResponseField name="format" type="'woff' | 'woff2'">
      字体文件格式。使用 `source` 字段时为必填项。
    </ResponseField>

    <ResponseField name="heading" type="object">
      专门覆盖标题的字体设置。

      <Expandable title="Heading">
        <ResponseField name="family" type="string" required>
          标题的字体族名称。
        </ResponseField>

        <ResponseField name="weight" type="number">
          标题的字体粗细。
        </ResponseField>

        <ResponseField name="source" type="string (uri)">
          字体来源的 URL，例如 `https://mintlify-assets.b-cdn.net/fonts/Hubot-Sans.woff2`，或标题所用本地字体文件的路径。如果指定的是 Google Font 的 `family` 名称，系统会自动加载 Google Fonts，因此无需提供来源 URL。
        </ResponseField>

        <ResponseField name="format" type="'woff' | 'woff2'">
          标题的字体文件格式。使用 `source` 字段时为必填项。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="body" type="object">
      专门覆盖正文文本的字体设置。

      <Expandable title="Body">
        <ResponseField name="family" type="string" required>
          正文文本的字体族名称。
        </ResponseField>

        <ResponseField name="weight" type="number">
          正文文本的字体粗细。
        </ResponseField>

        <ResponseField name="source" type="string (uri)">
          字体来源的 URL，例如 `https://mintlify-assets.b-cdn.net/fonts/Hubot-Sans.woff2`，或正文文本所用本地字体文件的路径。如果指定的是 Google Font 的 `family` 名称，系统会自动加载 Google Fonts，因此无需提供来源 URL。
        </ResponseField>

        <ResponseField name="format" type="'woff' | 'woff2'">
          正文文本的字体文件格式。使用 `source` 字段时为必填项。
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>