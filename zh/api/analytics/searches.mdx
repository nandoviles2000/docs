---
title: "获取搜索词"
description: "获取文档搜索词及其命中次数、点击率和点击量最高的页面。"
openapi: /zh/analytics.openapi.json GET /v1/analytics/{projectId}/searches
keywords: [ "分析", "搜索", "导出", "查询", "搜索词" ]
---

<div id="usage">
  ## 用法
</div>

使用此端点导出文档搜索分析数据。结果按命中次数降序排列，显示用户最常搜索的词条。

使用响应中返回的 `nextCursor` 参数对结果进行分页。当 `nextCursor` 不为 null 时，继续获取下一页结果。

<div id="filtering">
  ## 过滤
</div>

使用 `dateFrom` 和 `dateTo` 参数按日期范围过滤搜索数据。