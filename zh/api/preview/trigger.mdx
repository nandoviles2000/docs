---
title: "触发预览部署"
description: "为特定分支创建或更新预览部署，以便在合并到生产环境前查看文档更改。"
openapi: "/zh/openapi.json POST /project/preview/{projectId}"
keywords: [ "预览", "预览部署", "分支预览", "预发布" ]
---

<Info>
  预览部署仅适用于 [Pro 和 Enterprise 套餐](https://mintlify.com/pricing?ref=preview-deployments)。
</Info>

使用此接口可通过编程方式为 Git 分支创建或更新预览部署。如果指定分支的预览已存在，此接口会触发重新部署，而不是创建重复的预览部署。

响应中包含一个 `statusId`，你可以将其传给 [获取部署状态](/zh/api/update/status) 以跟踪部署进度。

<div id="use-cases">
  ## 用例
</div>

* **CI/CD 流水线**：在创建或更新拉取请求 (PR) 时，自动创建预览部署。
* **定时预览**：按计划从长期存在的功能分支构建预览。
* **自定义工具**：将预览创建集成到内部工作流或 Slack 机器人中。

<div id="rate-limits">
  ## 速率限制
</div>

此接口对每个组织的限制为每分钟最多 5 次请求。