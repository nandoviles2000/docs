---
title: "获取智能体任务"
description: "获取智能体任务的状态和详细信息，包括进度和拉取请求（PR）链接。"
openapi: /zh/admin-openapi.json GET /v2/agent/{projectId}/job/{id}
keywords: [ "智能体任务", "状态", "获取", "详细信息", "轮询" ]
---

轮询此端点以跟踪智能体任务的进度。`status` 字段会从 `active` 变为 `completed` 或 `failed`。

一旦智能体创建了拉取请求 (PR) ，`prLink` 字段就会填入相应链接。