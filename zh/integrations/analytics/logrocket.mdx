---
title: "LogRocket"
description: "将 LogRocket 与 Mintlify 文档集成，以回放用户会话、监控前端错误，并对交互问题进行详细调试。"
keywords: ["LogRocket 集成", "会话回放", "错误监控", "应用 ID"]
---

将以下内容添加到你的 `docs.json` 文件中，以将分析数据发送到 LogRocket。

```json Analytics options in docs.json
"integrations": {
    "logrocket": {
        "apiKey": "required"
    }
}
```