---
title: "图标"
description: "使用带有大小和颜色选项的 图标 组件，将 Font Awesome、Lucide、Tabler 或自定义来源的图标添加到文档页面中。"
keywords: ["Font Awesome", "Lucide", "Tabler", "SVGs"]
---

import IconsRequired from "/snippets/zh/icons-required.mdx";

使用 Font Awesome、Lucide、Tabler 的图标、SVG、外部 URL 或项目中的文件，为文档增添效果。

<Icon icon="flag" size={32} />

```mdx Icon example
<Icon icon="flag" size={32} />
```

<div id="inline-icons">
  ## 内联图标
</div>

当图标用于句子、段落或标题中时，会以行内方式显示。<Icon icon="flag" iconType="solid" /> 使用图标进行装饰或增强视觉效果。

```markdown Inline icon example
图标在句子、段落或标题中使用时以内联方式显示。<Icon icon="flag" iconType="solid" /> 使用图标进行装饰或增强视觉效果。
```

<div id="properties">
  ## 属性
</div>

<IconsRequired />

<ResponseField name="color" type="string">
  图标颜色，使用十六进制代码表示 (例如 `#FF5733`) 。
</ResponseField>

<ResponseField name="size" type="number">
  图标大小，以像素为单位。
</ResponseField>

<ResponseField name="className" type="string">
  应用于图标的自定义 CSS 类名。
</ResponseField>